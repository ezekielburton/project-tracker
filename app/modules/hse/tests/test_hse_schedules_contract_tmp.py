"""
TOMBSTONE — delete this file.

Written to the wrong filename by mistake; its real content lives in
test_hse_registers_contract.py. Left empty rather than holding a duplicate
copy of those tests, which would pass twice and drift apart.

Claude cannot delete files over the desktop bridge, so this has to go by
hand. Nothing imports it.
"""
